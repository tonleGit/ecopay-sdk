// describe('index', () => {
//   describe('myPackage', () => {
//     it('should return a string containing the message', () => {
//       const result = getVersion();

//       expect(result).toMatch(result);
//     });
//   });
// });
